__copyright__ = """
If not otherwise stated, any source code generated by Coco/R (other than Coco/R itself) does not fall under the GNU General Public License.
"""
import typing
from functools import wraps
from CoCoRuntime.parser import Parser
from CoCoRuntime.scanner import Position
from .Scanner import ScannerEnum


def _generateSet() -> typing.Tuple[typing.Tuple[bool, ...], ...]:
	T = True
	x = False
	return (T, x, T, T, T, T, x, T, x, x, x), (T, x, T, T, T, x, x, x, x, x, x)


class MyParser(Parser):
	__slots__ = ()

	@wraps(Parser.__init__)
	def __init__(self, *args, **kwargs):
		"""This ctor is mandatory, don't delete it: otherwise self.__class__.ENUM will be None"""
		super().__init__(*args, **kwargs)

	ENUM = ScannerEnum
	__main_production_name__ = "Test"
	__EOF_sym__ = ScannerEnum.EOF_SYM

	def pragmas(self) -> None:
		pass

	def Test(self) -> None:
		self.Expect(self.__class__.ENUM.a_Sym)
		while not self.StartOf(1):
			self.SynErr(11)
			self.Get()
		while self.la.kind == self.__class__.ENUM.b_Sym or self.la.kind == self.__class__.ENUM.c_Sym:
			if self.la.kind == self.__class__.ENUM.b_Sym:
				self.Get()
			else:
				self.Get()
		self.Expect(self.__class__.ENUM.d_Sym)
		self.A()

	def A(self) -> None:
		while not (self.la.kind == self.__class__.ENUM.EOF_SYM or self.la.kind == self.__class__.ENUM.e_Sym or self.la.kind == self.__class__.ENUM.g_Sym):
			self.SynErr(12)
			self.Get()
		if self.la.kind == self.__class__.ENUM.e_Sym:
			self.Get()
			self.Expect(self.__class__.ENUM.f_Sym)
		self.Expect(self.__class__.ENUM.g_Sym)

	set = _generateSet()
	errorMessages = "EOF expected", "a expected", "b expected", "c expected", "d expected", "e expected", "f expected", "g expected", "h expected", "i expected", "??? expected", "this symbol not expected in Test", "this symbol not expected in A"
