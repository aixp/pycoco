__copyright__ = """
If not otherwise stated, any source code generated by Coco/R (other than Coco/R itself) does not fall under the GNU General Public License.
"""
import typing
from functools import wraps
from CoCoRuntime.parser import Parser
from CoCoRuntime.scanner import Position
from .Scanner import ScannerEnum


def _generateSet() -> typing.Tuple[typing.Tuple[bool, ...], ...]:
	T = True
	x = False
	return (T, x, x, x, x, x, x, x, x, x, x), (x, T, T, x, T, T, T, T, T, T, T), (x, x, x, x, T, T, T, T, T, T, T), (x, T, T, T, T, x, T, T, T, T, T), (x, T, T, T, T, T, T, x, T, T, T), (x, T, T, T, T, T, T, T, T, x, T), (x, T, T, T, T, T, T, T, x, x, T)


class MyParser(Parser):
	__slots__ = ()

	@wraps(Parser.__init__)
	def __init__(self, *args, **kwargs):
		"""This ctor is mandatory, don't delete it: otherwise self.__class__.ENUM will be None"""
		super().__init__(*args, **kwargs)

	ENUM = ScannerEnum
	__main_production_name__ = "Test"
	__EOF_sym__ = ScannerEnum.EOF_SYM

	def pragmas(self) -> None:
		pass

	def Test(self) -> None:
		self.A()
		self.B()
		self.C()
		self.D()

	def A(self) -> None:
		while self.StartOf(1):
			if self.la.kind == self.__class__.ENUM.a_Sym:
				self.Get()
			elif self.StartOf(2):
				self.Get()
			else:
				self.Get()
		self.Expect(self.__class__.ENUM.c_Sym)

	def B(self) -> None:
		while self.StartOf(3):
			self.Get()
			self.Expect(self.__class__.ENUM.d_Sym)
		self.Expect(self.__class__.ENUM.e_Sym)

	def C(self) -> None:
		if self.StartOf(4):
			self.Get()
			self.Expect(self.__class__.ENUM.f_Sym)
		self.Expect(self.__class__.ENUM.g_Sym)

	def D(self) -> None:
		if self.StartOf(5):
			while self.StartOf(6):
				self.Get()
			self.Expect(self.__class__.ENUM.h_Sym)
		elif self.la.kind == self.__class__.ENUM.i_Sym:
			self.Get()
		else:
			self.SynErr(11)

	set = _generateSet()
	errorMessages = "EOF expected", "a expected", "b expected", "c expected", "d expected", "e expected", "f expected", "g expected", "h expected", "i expected", "??? expected", "invalid D"
